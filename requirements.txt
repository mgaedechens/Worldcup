# Core data stack
pandas>=2.2
numpy>=1.26

# Machine learning
scikit-learn>=1.5      # HistGradientBoostingClassifier, calibration, metrics

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Notebooks
jupyter>=1.0
ipykernel>=6.29

# HTTP (download raw datasets)
requests>=2.31

# Dev / quality (optional but recommended)
pytest>=8.0
ruff>=0.5

# --- v2 (commented until needed) ---
# streamlit>=1.36
